<% if @order.status == 'waiting' %>
  <p>配送が完了したら発送完了ボタンを押してください</p>
  <%= form_with(model: @order, local: true, url: user_product_transaction_path) do |form| %>

    <div class="form-group input-group-lg">
      
      <%= form.radio_button :status, :shipped, required:true %>
      <%= form.label :status, "発送済み", {value: :shipped} %>
    </div>
    <div class="form-group input-group-lg">
      <%= form.submit "送信", class: 'btn btn-primary form-control w-50 mt-2' %>
    </div>

  <% end %>
<% elsif @order.status == 'shipped' %>
  <h4>受け取りが完了するまでお待ちください</h4>
<% end %>