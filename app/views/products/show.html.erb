<div class="container-fluid">
  <div class="row">
    <% if current_user.products.any? %>
      <h3>Product (<%= current_user.products.count %>)</h3>
      <ol class="products">
        <%= render partial: 'product' %>
      </ol>
      <%= will_paginate @products %>
    <% end %>
  </div>
</div>